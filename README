This is the publican brand for OpenNMS

To install:

cd OpenNMS-Publican-Brand

Build it:
publican build --formats xml --langs all --publish

Install it:
sudo publican install_brand --path /usr/share/publican/Common_Content
